import { NextRequest, NextResponse } from "next/server";
const emailPattern=/^[^\s@]+@[^\s@]+\.[^\s@]+$/;
export async function POST(request:NextRequest){try{const body=await request.json();const email=typeof body.email==="string"?body.email.trim().toLowerCase():"";if(!emailPattern.test(email))return NextResponse.json({message:"Bitte gib eine gültige E-Mail-Adresse ein."},{status:400});const webhook=process.env.WAITLIST_WEBHOOK_URL;if(!webhook)return NextResponse.json({message:"Die Warteliste wird gerade aktiviert. Bitte versuche es in Kürze erneut."},{status:503});const response=await fetch(webhook,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,source:"originincome.com",createdAt:new Date().toISOString()})});if(!response.ok)throw new Error();return NextResponse.json({ok:true})}catch{return NextResponse.json({message:"Der Eintrag war gerade nicht möglich. Bitte versuche es nochmals."},{status:500})}}
