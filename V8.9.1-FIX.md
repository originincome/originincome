# Origin Income V8.9.1

Fix: Removed the explicit `payment_method_types` parameter from the Stripe Checkout Session. Managed Payments now selects the available payment methods automatically.

Upload this version to GitHub exactly like the previous versions. No changes are required in Stripe, Supabase, or Vercel environment variables.
